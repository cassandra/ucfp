*~
*.pyc
__pycache__
*.csv
.DS_Store
*.md
*.sh

.gitignore
/Makefile

/.git/
/.github/
/.claude/
/.private/
/venv/
# Exclude deploy/ noise from the build context, keeping only the docker build
# inputs the unified Dockerfile COPYs (the shared deploy/package/*). The lane
# scripts, compose/env templates, and env tooling stay out of the image context.
/deploy/*
!/deploy/package
/data/
/docs/
/design/
/src/venv/
/.flake8

/*.gz
/*.sqldump
